model Record {
  id        Int      @id @default(autoincrement())
  amount    Float
  type      String   // income / expense
  category  String
  note      String?
  createdAt DateTime @default(now())
}